import logging

logger = logging.getLogger("MyApp")
logging.basicConfig(level=logging.INFO)
logger.info("Processing data started.")
#code to perform data processing
logger.info("Processing data finished.")
